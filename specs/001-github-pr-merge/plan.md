# Implementation Plan: GitHub PR Merge Automation

**Branch**: `001-github-pr-merge` | **Date**: 2026-05-18 | **Spec**: [spec.md][spec]

**Input**: Feature specification from `specs/001-github-pr-merge/spec.md`

## Summary

Configure the PndTools GitHub repository to enforce fast-forward-only merges that preserve commit signatures, gate standard and Dependabot major-version PRs behind codeowner approval and passing CI checks, and automatically merge Dependabot minor/patch PRs individually per package ecosystem via Mergify's merge queue.

The solution combines configuration and a small GitHub Actions workflow: three GitHub Repository Rulesets enforce commit integrity, CI quality gates, and review gates; Mergify (free tier, full feature set on public repos) executes fast-forward merges via per-ecosystem queues; a Dependabot NuGet Fix workflow amends non-conventional commit messages and re-signs commits using a dedicated GitHub App (`milkshake-writer-bot`) before CI re-runs.

## Technical Context

**Language/Version**: N/A — configuration-only feature (YAML, GitHub UI)

**Primary Dependencies**:

- Mergify GitHub App (free tier, public repository)
- GitHub Repository Rulesets (GA)
- GitHub Dependabot version updates
- GitHub Actions (existing CI workflows)
- `milkshake-writer-bot` GitHub App — amends and re-signs Dependabot NuGet commits; credentials stored as Dependabot secrets (`MILKSHAKE_WRITER_BOT_CLIENT_ID`, `MILSHAKE_WRITER_BOT_APP_PRIVATE_KEY`)

**Storage**: N/A

**Testing**: Manual verification against acceptance scenarios in spec; no automated test suite for configuration

**Target Platform**: GitHub.com (public repository)

**Project Type**: Repository configuration / CI tooling

**Performance Goals**: Dependabot minor/patch PRs merge within 35 minutes of checks completing (SC-002)

**Constraints**:

- Commits on `main` must be signed (FR-003b) — rules out rebase and squash merge strategies
- Linear history required (FR-003a) — rules out merge commits
- Mergify fast-forward is the only strategy satisfying both constraints simultaneously — it advances the base branch pointer without creating new commits, so existing GPG signatures are preserved intact; rebase rewrites commits and is incompatible with the required_signatures ruleset
- Dependabot NuGet commits are unsigned and use non-conventional commit messages; a fix workflow using a GitHub App resolves both before CI re-runs
- Mergify queue branches are kept up to date via rebase (`update_method: rebase`) to avoid merge commits on the temporary queue branch

**Scale/Scope**: Single repository; three package ecosystems (NuGet, npm, GitHub Actions)

## Constitution Check

| Principle | Status | Notes |
| --- | --- | --- |
| I. Library-First | Pass | No .NET library code is added or changed |
| II. Zero-Warning Build | Pass | No .NET code changes |
| III. Test-First | Pass | No new public behaviour in the library |
| IV. No Shelling Out | Pass | No process invocation involved |
| V. Simplicity Over Abstraction | Pass | Minimal configuration; no custom workflows unless Mergify gaps require them |

## Project Structure

### Documentation (this feature)

```text
specs/001-github-pr-merge/
├── plan.md           ← this file
├── spec.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── mergify.yml       — reference Mergify configuration contract
│   ├── rulesets.md       — GitHub Ruleset definitions
│   └── dependabot.yml    — reference Dependabot configuration contract
└── tasks.md              — generated by /speckit-tasks
```

### Source Files (repository root)

```text
.mergify.yml                                    — Mergify queue and PR routing rules
CODEOWNERS                                      — already exists at repo root; verify coverage is complete
.github/
├── dependabot.yml                              — Dependabot ecosystem schedules and limits
└── workflows/
    └── dependabot-nuget-fix.yml                — amends Dependabot NuGet commit messages and re-signs via GitHub App
```

GitHub Repository Rulesets are configured via the GitHub UI or REST API and are not stored as files in the repository. A dedicated ruleset requiring the `Mergify Merge Protections` status check is needed in addition to the CI Quality Gates, Security Scanning, Review Gates, and Commit Integrity rulesets. Merge Protections must also be enabled in the Mergify dashboard before the check appears on PRs.

**Structure Decision**: Configuration-only; all changes are YAML files at the repository root or under `.github/`. No new source directories are needed.

## Complexity Tracking

No constitution violations. No unjustified complexity.

[spec]: spec.md
