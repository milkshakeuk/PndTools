# Copilot Instructions

**Before starting any task, read [CLAUDE.md]** — it is the single source of truth for all conventions, patterns, and constraints.

## Commands

```bash
dotnet build       # build all projects
dotnet test        # run all tests
dotnet format      # auto-fix style and licence-header violations
npm run lint:md    # lint markdown files
```

[CLAUDE.md]: CLAUDE.md
